[project]
name = "cs348k"
version = "0.1.0"
description = ""
authors = [
    {name = "JulienDarve",email = "jdarve@stanford.edu"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "transformers (>=4.49.0,<5.0.0)",
    "torch (>=2.11.0,<3.0.0)",
    "torchvision (>=0.26.0,<0.27.0)",
    "pillow (>=12.2.0,<13.0.0)",
    "numpy (>=1.26.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "cs348k", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.11,<3.14"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

